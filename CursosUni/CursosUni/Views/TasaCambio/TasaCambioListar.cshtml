@model IEnumerable<DAL.TasaCambio>

@{ ViewBag.Title = "TasaCambioListar";
    Layout = "~/Views/Shared/_Layout.cshtml"; }



<div class="page-header">
    <h1 class="text-center">Tasa de Cambio <small>Listado de Tasa de Cambio Mes Actual</small></h1>
</div>
<h2 class="page-header">
    <a href="~/TasaCambio/TasaCambioAdd" class="btn btn-success pull-right"> Nueva Tasa de Cambio </a>
</h2>



@using (Html.BeginForm("TasaCambioListar", "TasaCambio", FormMethod.Post, new { id = "frm-tgc" }))
{

<div class="panel-body">
    <div class="form-inline">
        @Html.Label("Rango de fechas")
        @Html.TextBox("iAno", null, new { @class = "form-control", id = "iAno" })
        @Html.TextBox("iMes", null, new { @class = "form-control", id = "iMes" })

    </div>
</div>
                <div class="text-right">
                    <button type="submit" class="btn btn-primary">
                        Buscar
                    </button>
                </div>
  }

@*@using (Html.BeginForm("Guardar", "TasaCambio", FormMethod.Post, new { id = "frm-tgc" }))
{

    foreach(var tc in Model)
                {

                    <text>
        <input type="hidden", name="@tc.Fecha" value="@tc.Fecha" />
        <input type="hidden" , name="@tc.Valor" value="@tc.Valor" />
</text>

                }

}*@


<table id="grid" class="table table-hover table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Valor)
            </th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Fecha)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Valor)
    </td>

    @*<td class="text-right">
        <a class="btn btn-primary" href="~/TasaCambio/TasaCambioVer/@item."> Visualizar </a>
        <a class="btn btn-success" href="~/TasaCambio/TasaCambioAdd/@item.ID"> Editar </a>*@
        @*<a onclick="return confirm ('Esta seguro de eliminar el registro')"
            class="btn btn-danger" href="~/Cursos/CursosVer/@item.id"> Eliminar </a>*@

        @*@Html.ActionLink("Edit", "Edit", new { id = item.id }) |
            @Html.ActionLink("Details", "Details", new { id = item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.id })*@
    @*</td>*@
</tr>}

    </tbody>



</table>





